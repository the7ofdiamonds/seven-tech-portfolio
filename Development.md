<p>I used React for the frontend and Vite to transpile the TypeScript code into JavaScript. During development, I used Sass to organize the styling and Gulp to compress the CSS file. The API is written in PHP and utilizes the MySQL database of the WordPress installation.</p>